version: '3.4'

services:
  poyecto4:
    image: poyecto4
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
